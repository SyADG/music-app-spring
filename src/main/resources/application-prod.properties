spring.datasource.url=jdbc:sqlserver://music-server.database.windows.net:1433;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;
spring.datasource.username=${USER_NAME}
spring.datasource.password=${USER_PASSWORD}
database=music-app-db

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=update
#spring.jpa.properties.hibernate.default_schema=music_app

#JWT Secret
jwt.secret=javainuse